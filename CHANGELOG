Version 0.1.0, 2022/11/18:
  - Full semver version string support
  - Version rules with the following operators:
    - `>=`
    - `<=`
    - `^=`
    - `~=`
    - `==`
    - `!=`
  - Dependency string parsing

Version 0.2.0, 2022/11/30:
  - Allow locator URIs in dependency strings
  - Add the `Release` type, which represents a particular package-version pair
  - Make `Version`, `Dependency`, and `Release` into subtypes of `RootObj` so
    that package users can subclass them

Version 0.3.0, 2022/??/??:
  - Add SPDX identifier support
